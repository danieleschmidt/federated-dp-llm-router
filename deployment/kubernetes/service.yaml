apiVersion: v1
kind: Service
metadata:
  name: federated-dp-llm-service
  namespace: federated-dp-llm
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: federated-dp-llm-api
  type: ClusterIP
